NexHop — IT Operations

1. npm install
2. npm run dev

Dark mode is the default theme. Light mode is available from the profile menu.
Language selection (PT/EN) is persisted in localStorage.

Supabase configuration remains in .env from the original project.
